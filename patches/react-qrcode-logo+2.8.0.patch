diff --git a/node_modules/react-qrcode-logo/dist/index.js b/node_modules/react-qrcode-logo/dist/index.js
index 540dff0..00296e5 100644
--- a/node_modules/react-qrcode-logo/dist/index.js
+++ b/node_modules/react-qrcode-logo/dist/index.js
@@ -187,7 +187,7 @@ var QRCode = /** @class */ (function (_super) {
         ctx.strokeStyle = fgColor;
         if (qrStyle === 'dots') {
             ctx.fillStyle = fgColor;
-            var radius = cellSize / 2;
+            var radius = cellSize / 1.5;
             for (var row = 0; row < length; row++) {
                 for (var col = 0; col < length; col++) {
                     if (qrCode.isDark(row, col) && !this.isInPositioninZone(row, col, positioningZones) && !(removeQrCodeBehindLogo && this.isCoordinateInImage(row, col, dWidthLogo, dHeightLogo, dxLogo, dyLogo, cellSize, logoImage))) {
